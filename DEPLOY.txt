1. 
winget install OpenJS.NodeJS.LTS
node -v
winget install Git.Git --accept-source-agreements --accept-package-agreements
git --version
rd /s /q "C:\FileServer"
git clone https://github.com/projectnumber135-droid/base_server.git "C:\FileServer"
cd c:\FileServer
npm install
cd ssl
node server.js
npm start

2. Open PowerShell as an Administrator and run:
New-NetFirewallRule -DisplayName "FileServer HTTP/HTTPS" -Direction Inbound -LocalPort 80,443 -Protocol TCP -Action Allow

3.
clear git repo files
git rm -r --cached .
git commit -m "Clear all files from repository"
git push origin main


update git
git add . && git commit -m "update files" && git remote add origin https://github.com/projectnumber135-droid/base_server.git || git remote set-url origin https://github.com/projectnumber135-droid/base_server.git && git push -u origin main


